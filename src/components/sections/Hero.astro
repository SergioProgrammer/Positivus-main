---
import Section from "./Section.astro";
import { Image } from "astro:assets";
import HeroIlust from "../../assets/pics/hero1.png";
import HeroGym from "../../assets/pics/gym.png";
---


<Section id="home">
  <div
    class="relative flex flex-col-reverse items-center md:flex-row mt-[-5rem] md:mt-[-10rem] pt-0 overflow-hidden" {/* Added overflow-hidden to prevent content spilling */}
    id="hero" 
    
  >
  <!--
    {/* Imagen de fondo (Gym) */}
    <Image
      src={HeroGym} {/* Use the imported variable directly */}
      alt="Fondo de gimnasio en casa"
      class="absolute top-0 left-0 w-full z-0 h-screen object-cover"
      loading="lazy"
    />
-->
    {/* Contenido (texto y botón) */}
    <div class="relative z-10 row items-center py-5 md:w-6/12 md:pb-20 md:pt-10 md:self-center bg-white bg-opacity-80 p-6 rounded-lg md:mx-4 lg:mx-8"> {/* Added relative positioning, z-index, and a translucent background for readability */}
      <div class="text-left space-y-3">
        <h1
          class="font-bold leading-none text-center text-3xl sm:text-5xl md:text-left md:text-7xl lg:text-8xl text-gray-900" {/* Added text color */}
        >
          ENTRENAMIENTO FÁCIL DESDE CASA
        </h1>
        <p
          class="mb-8 mt-6 text-center text-2xl font-normal leading-7 sm:mb-12 md:pr-12 md:text-left text-gray-700" {/* Added text color */}
        >
          Hacemos que ponerse en forma sea más fácil, pero el esfuerzo lo pones
          tú
        </p>
        <div
          class="mt-24 inline-flex w-full items-center justify-center md:justify-start"
        >
          <a
            href="#contact"
            target="_blank"
            class="rounded-2xl border bg-green px-8 py-5 text-center text-xl font-normal leading-7 text-black hover:bg-black hover:text-white"
          >
            Reserva una Consulta
          </a>
        </div>
      </div>
    </div>

    {/* Imagen principal (HeroIlust) */}
    <div class="relative z-10 md:w-5/6 w-2/3 flex justify-center items-center"> {/* Added relative positioning, z-index, and flexbox for better centering */}
      <Image
        src={HeroIlust}
        alt="Entrenamiento fácil desde casa"
        class="h-auto w-full object-contain md:max-w-none max-w-sm" {/* Changed object-cover to object-contain to prevent cropping, added max-w-sm for smaller screens */}
        loading="lazy"
      />
    </div>
  </div>
</Section>